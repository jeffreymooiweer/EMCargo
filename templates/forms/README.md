# Local form templates

Official PDF originals are not bundled. Import a supported file through
Administration → Document templates. Compatibility fingerprints and field
mappings live in `backend/app/config/document_templates.json`.

A local import records local-use provenance, not permission for public
redistribution. See `docs/licensing/template-migration.md` and
`compliance/third-party-manifest.json`.
